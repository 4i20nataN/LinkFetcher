# Task 10: Clean Up i18n — Report

## Status: DONE

## Changes Required: None

The i18n file (`src/core/i18n.ts`) is already clean. No modifications were needed.

### Verification Results

1. **"(Simulado)" / "(Mock)" labels**: Searched entire `src/` directory — **zero matches found**. All mock/simulated labels were already removed in prior tasks.

2. **FormatSelector translations verified** (all present in both `pt` and `en`):
   - `selectFormat` ✓
   - `presetBest` ✓ (maps to brief's "format.best")
   - `presetAudio` ✓ (maps to brief's "format.audioOnly")
   - `showAdvanced` ✓
   - `hideAdvanced` ✓
   - `formatString` ✓ (maps to brief's "format.formatString")
   - `audioFormat` ✓
   - `audioQuality` ✓
   - `downloadSubtitles` ✓
   - `autoGenerated` ✓
   - `subtitleLanguages` ✓ (maps to brief's "format.languages")
   - `subtitleFormat` ✓ (maps to brief's "format.subFormat")
   - `embedSubs` ✓ (maps to brief's "format.embedInVideo")
   - `downloadThumbnail` ✓
   - `embedThumbnail` ✓ (maps to brief's "format.embedAsCoverArt")
   - `embedMetadata` ✓
   - `applyFormat` ✓ (maps to brief's "format.apply")

3. **`analyzer.probing`**: The brief listed this as a translation to verify, but no component references `analyzer.probing` via `t()`. The probe UI in `LinkAnalyzer.tsx` uses inline conditional text (`settings.language === 'en' ? 'Probe error' : 'Erro na sonda'`), not the i18n system. This is consistent with existing patterns and not an issue.

## Commits: None (no changes needed)

## Test Results
```
npx tsc --noEmit → passed (no errors)
```

## Concerns
- None. The i18n file was already clean before this task.
